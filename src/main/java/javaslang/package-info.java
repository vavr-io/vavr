/**
 * Elementary Java extensions, like ensuring interface requirements at runtime, array and string
 * extensions, better print methods and stopping the JVM (even in the presence of deadlocks).
 * 
 * TODO
 */
package javaslang;
